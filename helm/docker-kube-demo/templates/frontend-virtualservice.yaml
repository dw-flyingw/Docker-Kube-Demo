{{- if .Values.ezua.enabled }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "docker-kube-demo.fullname" . }}-frontend
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "docker-kube-demo.labels" . | nindent 4 }}
    {{- include "hpe-ezua.labels" . | nindent 4 }}
spec:
  hosts:
    - "{{ .Values.ezua.virtualService.endpoint }}"
  gateways:
    - "{{ .Values.ezua.virtualService.istioGateway }}"
  http:
    - match:
        - uri:
            prefix: "/"
      route:
        - destination:
            host: {{ include "docker-kube-demo.fullname" . }}-frontend
            port:
              number: {{ .Values.frontend.service.port }}
{{- end }}
